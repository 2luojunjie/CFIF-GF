inherits: configs/default.yaml

dataset:
  name: IEMOCAP
  root: data/IEMOCAP
  num_classes: 4
  label_names: [angry, happy, neutral, sad]

model:
  num_classes: 4

train:
  learning_rate: 0.00002
  batch_size: 32
  epochs: 100
  optimizer: AdamW
